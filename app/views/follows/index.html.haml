%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @followings.each do |followed|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest('followed.email'.downcase)}?s=40")
            = link_to followed.full_name
          %td.extra-padding
            = followed.queue_items.count
          %td.extra-padding
            = followed.users_following.count
          %td.extra-padding
            = link_to follow_path(followed.id), method: :delete do
              %i.icon-remove
